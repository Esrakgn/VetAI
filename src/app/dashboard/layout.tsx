// This file's content has been removed to resolve a route conflict.
// The active layout is now managed in src/app/(main)/layout.tsx.
import React from 'react';

export default function RedundantLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
